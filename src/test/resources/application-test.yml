spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  flyway:
    enabled: true
    locations:
      - classpath:db/migration

api:
  security:
    token:
      secret: quero-ler-secret-para-teste-local-com-32
      issuer: quero_ler
      expiration-minutes: 120
